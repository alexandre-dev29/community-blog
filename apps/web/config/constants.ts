const API_URL = `${process.env.NEXT_PUBLIC_API_URL}`;
const FRONT_URL = `${process.env.NEXT_PUBLIC_FRONT_URL}`;
const SITE_NAME = `${process.env.NEXT_PUBLIC_SITE_NAME}`;

export {API_URL, FRONT_URL, SITE_NAME};
